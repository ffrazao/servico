package br.com.servico.negocio.assunto;
/*
import java.util.List;

import org.apache.commons.chain.Context;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Scope;
import org.springframework.stereotype.Service;
import org.springframework.util.CollectionUtils;

import br.com.servico.banco_dados.mysql.aterweb.dao.PessoaDao;
import br.com.servico.banco_dados.mysql.aterweb.modelo.pessoa.Pessoa;
import br.com.servico.negocio._BoModelo._VisualizarCmd;

@Service("PessoaVisualizarCmd")
@Scope("prototype")
public class VisualizarCmd extends _VisualizarCmd<Pessoa, Integer> {

	@Autowired
	public VisualizarCmd(PessoaDao dao) {
		super(dao);
	}

	@SuppressWarnings("unchecked")
	public boolean execute(Context context) throws Exception {
		super.execute(context);

		List<Pessoa> pessoaList = (List<Pessoa>) context.get("resposta");

		if (!CollectionUtils.isEmpty(pessoaList)) {
		}

		return false;
	}

}
*/